var paragraph = documnet.querySelector (p.paragraph);

let rainbow = document.querySelector(div#rainbow);
let dark = document.querySelector(div#dark);

function toggleClass(){
	let myBody = document.querySelector('body');

	if () {

	}
}